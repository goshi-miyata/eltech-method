# 構造化（分割）の原則

## 概要
ロジックはテスト単位で分割する

## ルール
1メソッドは1つの機能のみを持つ

## 理由
- テスト可能性向上
- 複雑性の削減
- 理解しやすくなる

## 判断基準
- テスト観点が1つか
- 複数の責務が混在していないか

## 例

### ✅ OK
if (A) func2(no);

### ❌ NG
if (A) {
    switch(no) {
        case 1: ...
    }
}
